RemindMe - simple project written in Java. In this project simply you can add a task and set timer to it with appopriate end message.
TimeConverter - java project. This project simply converts timezones, based to the current user timezone.
